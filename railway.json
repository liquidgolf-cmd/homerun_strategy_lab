{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run install:all && cd frontend && npm run build && cd ../backend && npm run build && mkdir -p public && cp -r ../frontend/dist/* public/"
  },
  "deploy": {
    "startCommand": "cd backend && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

